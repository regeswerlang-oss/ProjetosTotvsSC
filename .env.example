# Supabase — connection string do POOLER (porta 6543)
DATABASE_URL='postgresql://postgres.kpimalwnswxalwbidkog:SENHA@aws-1-us-east-2.pooler.supabase.com:6543/postgres'
# Tasks SC / API PCI (mesmo OAuth) — aspas SIMPLES por causa da barra
TASKS_USERNAME='actvs\reges.werlang'
TASKS_PASSWORD='sua_senha'
TASKS_SC_BASE_URL='https://api.tscst.com.br/restAPI'
# Assina o cookie de sessão:  python3 -c "import secrets;print(secrets.token_hex(32))"
SESSION_SECRET='troque_por_hex_64'
